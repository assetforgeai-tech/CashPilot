<?xml version="1.0"?>
<Container version="2">
  <Name>cashpilot</Name>
  <!-- :latest is DELIBERATE here, and only here.
       Everywhere else in this project pins a major.minor series, because
       SECURITY.md is right that a floating tag makes what you are running
       unknowable. Community Applications is the exception: CA is how unraid
       users receive updates at all, and a template pinned to 1.19 leaves every
       CA user on 1.19 until the template itself is re-published. That trades
       "unknowable version" for "silently frozen version", which is worse for
       an app whose whole job is to keep earning.
       Decided 2026-08-07. Do not "fix" this to a pinned tag without changing
       how CA users are meant to upgrade. See CashPilot-3c2n. -->
  <Repository>ghcr.io/assetforgeai-tech/cashpilot:latest</Repository>
  <Registry>https://github.com/assetforgeai-tech/CashPilot/pkgs/container/cashpilot</Registry>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <Support>https://github.com/assetforgeai-tech/CashPilot/issues</Support>
  <Project>https://github.com/assetforgeai-tech/CashPilot</Project>
  <Overview>CashPilot is a self-hosted passive income orchestrator. Deploy, manage, and monitor bandwidth-sharing, DePIN, storage, and GPU compute Docker containers from a single web dashboard. Tracks earnings across 49 services with historical charts and trend analysis.&#xD;
[br][br]&#xD;
This is the UI container. It provides the web dashboard, earnings collection, service catalog, and fleet management. It does NOT require Docker socket access.&#xD;
[br][br]&#xD;
[b]You also need the CashPilot Worker container[/b] on each server where you want to deploy passive income services. The worker manages Docker containers and reports back to this UI.&#xD;
[br][br]&#xD;
Set the same CASHPILOT_API_KEY on both the UI and all workers to authenticate them.&#xD;
[br][br]&#xD;
More info: https://github.com/assetforgeai-tech/CashPilot</Overview>
  <Category>Crypto: Tools: Productivity:</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/assetforgeai-tech/CashPilot/main/unraid/cashpilot.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/assetforgeai-tech/CashPilot/main/docs/icon.svg</Icon>
  <ExtraSearchTerms>passive income bandwidth sharing earnings money</ExtraSearchTerms>
  <Requires>CashPilot Worker container (cashpilot-worker) for container management</Requires>
  <Config Name="WebUI Port" Target="8080" Default="8080" Mode="tcp" Description="Port for the CashPilot web dashboard" Type="Port" Display="always" Required="true" Mask="false"/>
  <Config Name="Data" Target="/data" Default="/mnt/user/appdata/cashpilot" Mode="rw" Description="Database and configuration storage" Type="Path" Display="always" Required="true" Mask="false"/>
  <Config Name="TZ" Target="TZ" Default="America/New_York" Mode="" Description="Timezone (e.g. America/New_York, Europe/London)" Type="Variable" Display="always" Required="false" Mask="false"/>
  <Config Name="API Key" Target="CASHPILOT_API_KEY" Default="" Mode="" Description="Shared API key for worker authentication. Must match the key set on all CashPilot Worker instances." Type="Variable" Display="always" Required="true" Mask="true"/>
  <Config Name="Secret Key" Target="CASHPILOT_SECRET_KEY" Default="" Mode="" Description="Signing key for login sessions. Does NOT encrypt credentials — that is CASHPILOT_ENCRYPTION_KEY. Auto-generated if left empty." Type="Variable" Display="always" Required="false" Mask="true"/>
  <Config Name="Encryption Key" Target="CASHPILOT_ENCRYPTION_KEY" Default="" Mode="" Description="Fernet key encrypting stored credentials at rest. Auto-generated at /data/.fernet_key if left empty — back that file up. Set this only to restore a backup." Type="Variable" Display="always" Required="false" Mask="true"/>
</Container>

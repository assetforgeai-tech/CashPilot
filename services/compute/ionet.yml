name: io.net
slug: ionet
category: compute
status: active
website: https://io.net
description: >
  io.net is a decentralized GPU network for AI/ML workloads. Aggregates idle
  GPUs from consumers and data centers. Requires NVIDIA GPU with 8GB+ VRAM
  for meaningful earnings. Current worker onboarding is done from the io.net
  Cloud dashboard and installs worker/container components on the GPU host.
short_description: Decentralized GPU network for AI - NVIDIA GPU required

referral:
  signup_url: "https://io.net"

docker:
  image: ""
  platforms: []
  env: []
  ports: []
  volumes: []
  command: ""
  network_mode: ""

requirements:
  residential_ip: false
  min_bandwidth: ""
  gpu: true
  min_storage: ""

payment:
  methods: [crypto]
  minimum_payout: ""
  currency: IO
  frequency: "Epoch-based"

earnings:
  monthly_low: 0
  monthly_high: 500
  currency: USD
  per: "GPU"
  notes: "Requires NVIDIA GPU with 8GB+ VRAM. RTX 4090 claims of $500-1000/mo (likely lower in practice)."

cashout:
  method: manual
  dashboard_url: "https://cloud.io.net"
  # Undocumented: the provider does not publish a minimum. NOT 0 —
  # 0 means 'no minimum, cash out anything', which would tell the
  # user they can withdraw now and disables payout detection.
  min_amount: null
  currency: IO

platforms: [linux]

collector:
  type: manual
  notes: "Dashboard at cloud.io.net. Current worker setup is dashboard-driven; public earnings API/sample response is not documented."

payout:
  model: unknown
  notes: "Not yet determined. Flagged rather than shown blank -- see CashPilot-luj."

name: URnetwork
slug: urnetwork
category: bandwidth
status: active
website: https://ur.io
description: >
  URnetwork is a decentralized VPN and bandwidth-sharing network. You earn by
  providing bandwidth as a community provider. Uses JWT-based authentication.
  Official Docker image from Bring Your Own (bringyour). Supports both direct
  mode (with tun device) and proxy mode (SOCKS5).
short_description: Decentralized VPN/bandwidth - JWT auth, direct or proxy mode

referral:
  signup_url: "https://ur.io/?referral_code=1Q3G19"
  code: "1Q3G19"

docker:
  image: bringyour/community-provider
  platforms: [linux/amd64, linux/arm64]
  resources:
    mem_limit: "128m"
    oom_score_adj: 300
  env:
    - key: UR_AUTH_TOKEN
      label: "Auth Token"
      required: true
      secret: true
      description: "Log in at <a href=\"https://app.urnetwork.com\">app.urnetwork.com</a>, press F12 -> Application -> Local Storage -> click <code>https://app.urnetwork.com</code> -> copy the <code>auth_token</code> value (a JWT starting with eyJ)"
  ports: []
  volumes:
    - "urnetwork-data:/root/.urnetwork"
  command: "provide"
  network_mode: ""

requirements:
  residential_ip: false
  min_bandwidth: ""
  gpu: false
  min_storage: ""

payment:
  methods: [crypto]
  minimum_payout: "$5"
  currency: USD
  frequency: "On request"

earnings:
  monthly_low: 0
  monthly_high: 5
  currency: USD
  per: "device"
  notes: "Works on VPS and residential. Crypto payouts. Supports proxy mode for multi-IP setups."

cashout:
  method: redirect
  dashboard_url: "https://urnetwork.com/dashboard"
  min_amount: 5.0
  currency: USD

platforms: [docker, windows, macos, linux]

collector:
  type: manual
  notes: "Logged-in client manager exposes Clients, Statistics, Providers, Wallet Stats and Account Settings. Statistics/Wallet endpoints returned 401 in the audited browser session, so no collector/API contract is confirmed."

payout:
  model: external
  chain: solana
  address_source: app
  notes: "Wallet is linked inside the URnetwork app. AN UNLINKED WALLET FORFEITS PAYOUT AFTER 30 DAYS."

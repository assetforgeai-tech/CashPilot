name: Earn.fm
slug: earnfm
category: bandwidth
status: active
website: https://earn.fm
description: >
  Earn.fm pays you for sharing your internet bandwidth. The deployable client
  uses a UUID API key from Account Settings; the earnings collector still uses
  the dashboard email/password login flow because no stable public balance API
  key contract is documented.
short_description: Bandwidth sharing - API-key client, lightweight Docker

referral:
  signup_url: "https://earn.fm/ref/GEISYB91"
  code: "GEISYB91"

docker:
  image: earnfm/earnfm-client
  platforms: [linux/amd64, linux/arm64]
  resources:
    mem_limit: "128m"
    oom_score_adj: 200
  env:
    - key: EARNFM_TOKEN
      label: "API Key"
      required: true
      secret: true
      description: "Log in at <a href=\"https://app.earn.fm\">app.earn.fm</a> → click your profile icon (top right) → Account Settings → copy the API Key (UUID format)"
  ports: []
  volumes: []
  command: ""
  network_mode: ""
  privileged: false

requirements:
  residential_ip: true
  vps_ip: true
  devices_per_ip: 1
  min_bandwidth: ""
  gpu: false
  min_storage: ""

payment:
  methods: [crypto]
  minimum_payout: "$3"
  currency: USD
  frequency: "On request"

earnings:
  monthly_low: 0
  monthly_high: 3
  currency: USD
  per: "device"
  notes: "Relatively new platform. Earnings vary by location and bandwidth availability."

cashout:
  method: redirect
  dashboard_url: "https://app.earn.fm/"
  min_amount: 3.0
  currency: USD

platforms: [docker, linux]

collector:
  type: api
  notes: "Supabase email/password auth. Docker client uses separate EARNFM_TOKEN UUID API key for bandwidth sharing only."
  credential_hint: "Use your Earn.fm account email and password (same as <a href='https://app.earn.fm' target='_blank'>app.earn.fm</a> login)."

payout:
  model: internal
  notes: "Balance held by Earn.fm; withdrawn on request."

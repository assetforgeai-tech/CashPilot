name: Dawn Internet
slug: dawn
category: depin
status: active
website: https://dawninternet.com
description: >
  Dawn Internet is a decentralized wireless network that rewards users for sharing
  their internet connection. Available as a browser extension and a dedicated
  hardware box (Dawn Node). The extension passively earns Dawn Points while you
  browse. The network aims to build a decentralized ISP layer for underserved areas.
short_description: DePIN wireless network - browser extension and hardware box

referral:
  signup_url: "https://dawninternet.com/?code=2QLQV97F"
  code: "2QLQV97F"

docker:
  image: ""
  platforms: []
  env: []
  ports: []
  volumes: []
  command: ""
  network_mode: ""

dashboard:
  credentials:
    - key: dashboard_session
      label: "DAWN dashboard session"
      kind: cookie
      secret: true
      required: false
      source: "https://dashboard.dawninternet.com logged-in browser session"
      description: "Optional cookie/session material for future dashboard scrape. Dawn currently has no confirmed headless deploy credential."

requirements:
  residential_ip: true
  min_bandwidth: ""
  gpu: false
  min_storage: ""

payment:
  methods: [crypto]
  minimum_payout: ""
  currency: DAWN
  frequency: "Token airdrop (TBA)"

earnings:
  monthly_low: 0
  monthly_high: 5
  currency: USD
  per: "device"
  notes: "Earns Dawn Points pre-token launch. Actual value depends on future token price. Hardware box earns more than browser extension."

cashout:
  method: manual
  dashboard_url: "https://dashboard.dawninternet.com"
  # Undocumented: the provider does not publish a minimum. NOT 0 —
  # 0 means 'no minimum, cash out anything', which would tell the
  # user they can withdraw now and disables payout detection.
  min_amount: null
  currency: DAWN
  notes: "Extension works but half-baked. Black Box hardware announced. Check future."

platforms: [browser-extension]

collector:
  type: scrape
  credentials:
    - key: dashboard_session
      label: "DAWN dashboard session"
      kind: cookie
      secret: true
      required: true
      source: "https://dashboard.dawninternet.com logged-in browser session"
      description: "Logged-in cookie/session string used by CashPilot to read Dawn rewards and connection state."
  notes: "Logged-in dashboard exposes connection quality, connected state, total points, epoch points, referral points, streak, and earnings tables. No public API or headless node setup confirmed."

payout:
  model: unknown
  notes: "Not yet determined. Flagged rather than shown blank -- see CashPilot-luj."

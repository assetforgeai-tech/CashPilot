[Unit]
Description=Record an NKN ChainDB publisher failure

[Service]
Type=oneshot
ExecStart=/usr/bin/logger -p daemon.err -t cashpilot-nkn-chaindb "NKN ChainDB publisher failed; inspect the service journal"
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true

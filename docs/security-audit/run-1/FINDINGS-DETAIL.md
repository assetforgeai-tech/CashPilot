# Finding details

No medium-or-higher findings survived validation. The Unix helper socket
authority is documented as a deployment hardening note because access is
deliberately equivalent to Docker host administration, not an authorization
bypass.

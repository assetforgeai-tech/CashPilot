# CP-014K evidence — ProxyBase.xyz normalization

## Reference audit

Compared against `D:\1. WORK_true\CashPilot\provider-runtime\provider_code_setup_node\proxybase.xyz.py`.
The official sequence remains wallet import, login, and foreground seller start.
CashPilot accepts exactly the `phrase` deploy credential; no API key or
collector credential is added.

## Changes

- Pin ProxyBase CLI to release `v0.1.47` and verify the Linux amd64 binary SHA-256
  before placing it in the runtime image. No `latest` tag or unverified installer
  pipe remains in the image build.
- Keep the wallet/seller state in the named `proxybase-xyz-data` volume.
- Import the phrase once per persistent volume, mark completion, then clear the
  phrase environment variable before login/seller start.
- Keep one foreground seller process and Docker `restart: always` semantics;
  no host systemd + container duplicate is started by this path.
- Change topology from manual to `slot_both`, matching direct + proxy lanes and
  the 20-slot deployment contract. Collector remains `count_only`; no earnings
  API was added.

## Verification

- `python -m pytest tests/test_provider_installers.py tests/test_catalog_loader.py -q` — 39 passed.
- `uv run ruff check` — pass.
- `uv run ruff format --check` — pass.
- `python -m compileall -q app tests` — pass.
- `git diff --check` — pass.
- No Azure, VPS, provider dashboard, or production mutation.

## Remaining operator check

The real provider dashboard must confirm the expected direct/proxy node count
after deployment. This task does not perform that live check.

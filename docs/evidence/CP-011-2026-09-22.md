# CP-011 read-only Azure canary preflight

Date: 2026-09-22  
Worktree: `repo-cp011-20260922`  
Branch: `task/cp011-disposable-azure-canary`  
Base: `b637fde76ab2b5e31b16b6a8d4a9c3b9335e108d` (`origin/main` at preflight)

## Decision

Preflight is **PASS for inventory** and **BLOCKED for canary mutation**. No VM,
network, worker, provider, dashboard, SSH, reboot, Run Command, fault injection,
deployment, or cleanup operation was performed by CP-011.

The Azure CLI session is authenticated, but the repository inventory does not
contain the referenced credential file and the live-test resource group has no
explicit `disposable=true` or CP-011 ownership tag. Existing evidence identifies
the resources as the two owner-authorized live-test workers, while some reports
call them live/production workers. This ambiguity must be resolved before any
guest or provider mutation.

## Read-only evidence

- Azure CLI: `2.87.0`.
- Subscription: `0e4b9f20-f92f-4883-a598-3251b0016d65`, enabled and selected.
- Tenant: `70716a5a-7984-475b-b9eb-b68b902b4409`.
- Current Azure principal: `Marialabarbera@vidgenx.onmicrosoft.com`.
- Resource group: `rg-cashpilot-live-test-20260911`, provisioning `Succeeded`.
- VM `cashpilot-live-ea`: East Asia, `Standard_D8s_v4`, Ubuntu 24.04 Gen2,
  running, provisioning `Succeeded`.
- VM `cashpilot-live-je`: Japan East, `Standard_D8s_v4`, Ubuntu 24.04 Gen2,
  running, provisioning `Succeeded`.
- OS disks: `osdisk-cashpilot-live-ea` and `osdisk-cashpilot-live-je`,
  `512 GB`, `Premium_LRS`, attached.
- NICs: `nic-cashpilot-live-ea` and `nic-cashpilot-live-je`; accelerated
  networking and IP forwarding enabled; 10 IPv4 configurations per NIC.
- Public IPs: 20 Standard, static IPv4 resources, 10 per VM; slot map in
  `D:\1. WORK_true\CashPilot\azure_live_test_inventory.json`.
- NSGs: `nsg-cashpilot-ea` and `nsg-cashpilot-je` currently allow all inbound
  TCP and UDP from Internet. This is test-only exposure and is not a CP-011
  acceptance result.
- Azure role lookup: current principal has `Owner` inherited at subscription
  scope; no resource lock is present.
- Credential reference: `C:\Users\KALINH\.config\cashpilot\azure-worker-credentials`
  is absent. No credential value was read, copied, logged, or committed.

## Existing evidence boundary

Repository evidence records these two VMs as the clean live-test scope and
records prior worker IDs `118903` and `118904`. It also explicitly says that
provider dashboard, packet-level, one-hour recovery, and authenticated runtime
observations require a fresh canary. Historical evidence is not reused as fresh
CP-011 acceptance.

## Required authorization to continue

Confirm that CP-011 may mutate **only** resource group
`rg-cashpilot-live-test-20260911`, VMs `cashpilot-live-ea` and
`cashpilot-live-je`, and their attached disposable canary resources. Confirm
that existing provider containers/data on those VMs may be inspected and, if
needed, changed within the canary plan. Without this explicit scope decision,
CP-011 remains blocked at the safe preflight gate.

## Planned (not executed) canary sequence

1. Capture immutable Azure/NIC/NSG/worker/provider baseline.
2. Run deterministic dry-run and verify allowlisted worker/runtime/provider
   matrix.
3. Deploy one disposable canary lane only through the normal API.
4. Capture DNS/DoH, IPv6, UDP, direct-fallback, and egress packet evidence.
5. Inject watchdog failure; verify fail-closed and staged replacement.
6. Reboot the worker; verify worker, slot map, runtime persistence, and
   dashboard reconciliation.
7. Remove only disposable resources; verify leases, containers, and authority
   state converge to baseline.
8. Record rollback commands and machine-readable gate results.

No step above was executed beyond read-only Azure control-plane queries.

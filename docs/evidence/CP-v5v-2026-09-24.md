# CP-v5v — proxy rotation test clock

Date: 2026-09-24
Branch: `chore/compose-pin-1.67-20260923`

## Failure and cause

CI for PR #501 and the first run of PR #502 exposed four failures in
`tests/test_proxy_pool_state.py`. The tests claimed requests at the fixed time
`2026-09-24 00:00:00`, while new requests use SQLite `datetime('now')`. After
the runner passed that instant, the claim correctly returned `None`.

## Change

Use a fresh UTC anchor one minute after request creation and relative offsets
for lease recovery. Fencing, serialization, probe-generation, and CAS
replacement assertions remain unchanged. No database or runtime code changed.

## Verification

- Before: four focused cases failed on a fresh run (`None` claim).
- After: `python -m pytest -q tests/test_proxy_pool_state.py`: 14 passed.
- `uv run ruff check tests/test_proxy_pool_state.py`: PASS.
- `uv run ruff format --check tests/test_proxy_pool_state.py`: PASS.
- `git diff --check`: PASS.

No Azure, SSH, provider, proxy, wallet, lease, or production mutation.

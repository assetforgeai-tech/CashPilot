# CP-v5v — proxy rotation test clock

Date: 2026-09-24
Branch: `fix/proxy-rotation-test-clock-20260924b`

## Failure and cause

PR #501 and #502 CI both failed after release `v1.67.0` became current. The
compose pin mismatch is handled by PR #502. A second independent failure was
four `test_proxy_pool_state.py` cases claiming requests at the fixed time
`2026-09-24 00:00:00`; new requests use SQLite `datetime('now')`. Once the
runner clock passed that instant, the claim returned `None`.

## Change

Use a fresh UTC anchor one minute after request creation and relative offsets
for lease recovery. Keep the assertions for serialization, token fencing,
probe generation, and CAS replacement. No database or runtime code changed.

## Verification

- Before: four focused cases failed on a fresh local run (`None` claim).
- After: `python -m pytest -q tests/test_proxy_pool_state.py`: 14 passed.
- `uv run ruff check tests/test_proxy_pool_state.py`: PASS.
- `uv run ruff format --check tests/test_proxy_pool_state.py`: PASS.
- `git diff --check`: PASS.

Full suite and CI are pending the stacked compose-pin branch from PR #502.
No Azure, SSH, provider, proxy, wallet, lease, or production mutation.

# CP-015F evidence — immutable proxy-route release gate

Date: 2026-09-24
Branch: `task/cp015f-release-20260924`
Base: `origin/main` at `556993cb8e308063c3babc157ef4bdd0d1308036`

## Scope

- Completed the release-manifest follow-up required by CP-015D and CP-015E.
- Added an immutable `sing-box` GHCR artifact beside the CashPilot UI and worker
  artifacts.
- Kept the sidecar digest aligned with `app/orchestrator.py`'s
  `SING_BOX_IMAGE_PIN`; a drifted registry result fails closed.
- Updated manifest verification so container-image entries validate their
  `@sha256` reference and do not pretend that an image digest is a local file
  SHA-256.
- No Azure, SSH, Docker pull, provider, proxy, wallet, lease, account-pool, or
  production mutation was performed.

## Changes

- `scripts/create_release_runtime_manifest.py`
  - requires `--sidecar-digest`;
  - emits UI, worker, and `sing-box` container-image entries;
  - rejects a sidecar digest that differs from the worker source pin.
- `.github/workflows/release.yml`
  - resolves the pinned `ghcr.io/sagernet/sing-box:v1.12.0` digest in normal
    and recovery publication paths.
- `release/runtime-manifest.schema.json`
  - declares `file` and `container-image` artifact kinds.
- `tools/verify-runtime-manifest.py`
  - validates container-image reference/digest equality;
  - records immutable image references without reading a same-named local file;
  - preserves the previous active pointer on failure.
- `docs/ops/release-checklist.md`
  - documents image-vs-file verification behavior.
- Focused regression tests cover required sidecar input, digest pin drift,
  immutable reference validation, mismatch rejection, and active-pointer
  preservation.

## Verification

| Check | Result |
|---|---|
| `python -m pytest -q` | PASS — 3582 passed, 7 skipped |
| `uv run ruff check .` | PASS |
| `uv run ruff format --check .` | PASS — 564 files already formatted |
| `python -m compileall -q app tests scripts tools` | PASS |
| `git diff --check` | PASS |
| Focused manifest/release tests | PASS — 19 passed |

## Disposable manifest dry-run

- Candidate: synthetic `v1.69.0`, rollback `v1.68.0`, three artifacts.
- Sidecar digest: immutable digest read from the worker source pin and carried
  into the candidate manifest.
- Candidate verification: PASS.
- Tampered sidecar digest: rejected with exit code `1`.
- Active pointer after tamper: remained `v1.69.0`.
- Synthetic rollback manifest: verification PASS at `v1.68.0`.
- No registry pull was attempted because Docker is not installed in this local
  shell; immutable pull remains a release/worker operation, not a CP-015F local
  mutation.

## Gate status

CP-015F implementation and local release/rollback verification: **PASS**.

This does not create a GitHub release, deploy a worker, or authorize CP-013.
Production rollout remains separately approval-gated.

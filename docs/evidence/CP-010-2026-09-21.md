# CP-010 Evidence Index

- Baseline: `origin/main` at `9e0bf1c2f4bb837ace39b39829af4b7b8f1cc7b1` after rebase.
- Scope: read-only security and operations audit. No production, Azure, VPS, SSH, worker, or provider mutation.
- Detailed security evidence: `docs/evidence/CP-010-security.md`.
- Detailed UI/operations evidence: `docs/evidence/CP-010-ui-ops.md`.
- Findings: 3 source-backed operations/security findings; fixes remain separate scoped tasks.
- Live-resource checks: `INCONCLUSIVE` where Azure NSG, GHCR policy, repository protection, or deployed runtime access was unavailable; no provider behavior inferred.
- Shared Beads authority: `C:\Users\KALINH\\.beads-planning\\.beads`; `CP-010` claimed, not closed.

## Verification

Commands run in this worktree:

```text
git fetch origin main --prune
git rebase origin/main
uv run pytest
uv run ruff check .
uv run ruff format --check .
python -m compileall -q app tests
git diff --check
```

Results are recorded in `docs/evidence/CP-010-security.md`; no commit, push, merge, PR, or issue close performed.

Fresh gate results after the CP-001 rebase:

```text
pytest: 3367 passed, 7 skipped in 230.40s
ruff check: All checks passed!
ruff format --check: 490 files already formatted
compileall: exit 0
git diff --check: exit 0
```

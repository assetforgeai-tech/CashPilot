# CP-009 Lease/Heartbeat Capacity Reconciliation

## Scope

- Shared Beads issue: `CP-009`
- Branch: `task/cp009-lease-heartbeat-capacity`
- No SSH, Azure, VPS, provider, proxy, or production mutation.
- Owned files only: database lease/capacity authority, topology heartbeat grouping,
  provider automation counters, and focused tests.

## Changes

- Added read-only `reconcile_provider_proxy_leases()`; active leases are compared
  with non-retired provider instances and orphan leases are reported, never
  released implicitly. Repeated calls are idempotent.
- Added authoritative `used` and `remaining` capacity aliases beside legacy
  `leased` and `available` fields.
- Added deterministic proxy heartbeat grouping: direct lanes are excluded;
  EarnApp and Pawns/IPRoyal remain isolated; other proxy lanes use the shared
  proxy group.
- Added secret-free capacity counter normalization.

## Verification

- TDD red test observed: missing grouping/counter/reconciliation symbols failed.
- Focused CP-009 tests: `6 passed`.
- Focused regression set: `108 passed`.
- Combined lease/capacity/reconciliation subset: `29 passed`.
- `uv run ruff check app/database.py app/provider_topology.py app/provider_automation.py tests/test_cp009_lease_heartbeat.py`: pass.
- `uv run ruff format --check ...`: pass.
- `python -m compileall -q app tests`: pass.
- `git diff --check`: pass.

Full suite: `3395 passed, 7 skipped, 2 failed`. Both failures are the known
compose-release pin gate: the base branch pins `1.56` while release metadata in
this branch still reports `1.54`; CP-009 does not own compose or release files.

## Remaining gate

Full repository verification, commit, push, PR, and required CI remain before
CP-009 can be closed. Merge/close is not performed by this task.

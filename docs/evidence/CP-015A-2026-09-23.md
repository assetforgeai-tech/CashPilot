# CP-015A Evidence — Proxy Route and State Contracts

Date: 2026-09-23
Task: CP-015A
Base: `origin/main` at `341b554c3cc6918a4f551abc4b2c5a383b0e4aac`

## Scope

- Added the shared proxy-route contract.
- Added the Proxy Pool state-machine contract.
- Extended the provider policy matrix with the frozen direct-only, hybrid, and
  proxy-only groups.
- Added machine-checked contract tests.

EarnApp account/link/recovery behavior and the Pawns/IPRoyal private allocator
remain separate. Direct-only Mysterium/NKN do not receive proxy leases.

## Verification

The following commands were run in this worktree:

```text
python -m pytest -q tests/test_proxy_route_contract.py
```

Result: `3 passed` (RED first: three expected failures for missing contract docs/matrix).

The local full suite initially reported two compose-pin failures because the
local isolated `refs/fork-tags` namespace stopped at `v1.65.0` while the fork
already published `v1.66.0`. Fetching the workflow's exact fork-tag refspec
resolved the local baseline; `tests/test_compose_image_pins.py` then reported
`24 passed`. No compose file or release tag was changed.

Additional repository gates are recorded in the task handoff and PR checks.

## Safety

- No Azure, SSH, VPS, provider, proxy, wallet, lease, runtime, or production
  mutation.
- No credentials, tokens, cookies, passwords, wallets, database contents, or
  raw provider responses included.
- No existing provider allocator or runtime implementation changed.

## Remaining work

CP-015B must not start until this task is merged with green CI and CP-015A is
closed in the shared Beads database.

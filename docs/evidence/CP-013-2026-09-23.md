# CP-013 staged rollout evidence - 2026-09-23

## Stage 3: East Asia worker canary

- Scope: `cashpilot-live-ea` only; `cashpilot-live-je` unchanged.
- Release: `v1.65.0`; rollback: `v1.64.0` / prior worker `1.53.17`.
- Image digest verified before rollout: `sha256:0f11def30bf36ce1a872e7d2e67b2c35a99b292f5b35fc071aee7606c8a0bd11`.
- Compose direct digest reference was rejected by the VM's Compose path, so the digest-verified image was loaded under the local immutable alias `cashpilot-worker:cp013-v1.65`.
- First verification attempt was invalid because the operator script called `/health`; the worker contract is `/healthz`. No production failure was inferred from that bad probe.
- The first alias rollout was rolled back without deleting volumes or provider containers.
- Corrected retry result: old worker `2a34574d167f` replaced by `3ad3b651e81d`; status `running`; Docker health `healthy`; version `v1.65.0`; `/healthz` returned `{"status":"ok"}`.
- Running container count remained `286` before and after; no provider container was removed.
- Heartbeat after retry: worker logged authenticated `POST /api/workers/heartbeat` with HTTP `200`.
- JE was not mutated.

## Stage decision

Stage 3 passed. Stop before any JE rollout or further production mutation. Next stage requires a separate JE compose/runtime audit because JE currently uses a custom `dev` image and historical override layers.

## Outstanding

- Rotate the API key observed in diagnostic output before final acceptance.
- Collect representative direct/proxy egress, DNS, IPv6, UDP and lease evidence before declaring production-ready.

## Stage 4: Japan East read-only audit

- Scope: `cashpilot-live-je` only; no mutation performed.
- Worker is `cashpilot-phase7-worker:20260921`, `dev`, healthy; running container count `285`.
- Compose uses `docker-compose.worker.yml` plus `1.53.27`, proxy-canary, proxy-shellless, proxybase-fix and CP-011 pinned overrides. This is not equivalent to EA's two-file path.
- The current JE worker exposes no `/healthz` response (`404`), so the v1.65 health contract cannot be assumed from the current image.
- The base compose embeds the existing UI URL, worker identity/URL, API key and runtime mounts. These must be preserved during any migration.
- Stage 4 result: audit complete; stop before JE mutation. A separate temporary v1.65 alias override must be rendered from the full effective JE stack, with a long enough wait for heartbeat, then verified and rollback-tested.

## Stage 5: Japan East worker canary

- Scope: `cashpilot-live-je` only; EA unchanged.
- Digest verified: `sha256:0f11def30bf36ce1a872e7d2e67b2c35a99b292f5b35fc071aee7606c8a0bd11`.
- The complete effective JE override stack was preserved; only a final temporary alias override changed the worker image.
- Worker changed from `3fa7a84c9091` to `18ad0aa90f72`; image `cashpilot-worker:cp013-v1.65-je`; status `running`; Docker health `healthy`; version `v1.65.0`; `/healthz` returned `{"status":"ok"}`.
- Container count remained `285 -> 285`; no provider container was deleted. Authenticated heartbeat returned HTTP `200` with `acks=3`.
- One Repocket provider container was observed restarting during the post-rollout snapshot; its egress sidecar remained up. This is provider-runtime evidence requiring separate investigation, not a worker rollout failure.
- Stage 5 result: worker rollout passed. Stop before any further mutation and investigate the Repocket restart separately.

## Stage 6: Repocket proxy-005 repair

- Scope: `cashpilot-live-je`, one broken Repocket runtime only.
- Root cause: the container used `entrypoint=["/bin/sh", "-c", ...]`, but `repocket/repocket` is shell-less. It exited `127` continuously with `exec /bin/sh failed` while its egress sidecar stayed running.
- The v1.65 renderer already contains the shell-less Repocket contract and regression test; the live container predated that fix.
- Recreated only `cashpilot-repocket-proxy-w118904-proxy-005` from its Docker metadata, retaining image, provider credentials, labels, restart policy and existing sidecar network namespace. Proxy/account inventory was not deleted or changed.
- New container `684c11b85e1d`; native entrypoint `/usr/local/bin/repocket`; status `running`; exit `0`; restart count `0`.
- Runtime log confirms SDK `1.2.0`, registration, stable device id `6ab347c2b708d4001181bf06`, and `peer connected`.
- Stage 6 passed. Stop before broader traffic/network/lease verification.

## Stage 7: acceptance sweep

- Both EA and JE workers remain `v1.65.0`, `running`, Docker `healthy`; recent authenticated heartbeats returned HTTP `200`.
- JE Repocket-005 remains `running`, restart count `0`, native entrypoint, and logs show `peer connected`.
- JE proxy sidecars show the intended fail-closed shape: TUN `auto_route`, `strict_route`, DNS hijack, DoH over `proxy-out`, IPv4-only DNS strategy, no direct final route, and IPv6 only link-local TUN route.
- A Repocket sidecar probe completed through the proxy and returned an egress address; its sing-box logs recorded DNS and outbound proxy connections. This proves traffic for that lane, not every provider lane.
- An EarnFM proxy sidecar timed out on the same public-IP probe while DNS resolution and proxy DNS exchange succeeded. This is a provider/proxy-path acceptance failure requiring investigation; no rotation or destructive action was taken.
- Stage 7 result: acceptance incomplete; stop before changing the EarnFM lane. API-key rotation and full provider/lease/dashboard evidence remain outstanding.

## Stage 7 follow-up: JE EarnFM proxy-010 diagnostics

- Read-only inspection on `cashpilot-live-je`; main container and egress sidecar remained running, sidecar restart count `0`.
- Sing-box config validation passed. Routing remains fail-closed: final route `proxy-out`; DoH server uses `proxy-out`; DNS strategy `ipv4_only`; firewall default policy `drop`; only the TUN interface and exact proxy endpoint are allowed; the sole `direct` route rule matches only the proxy endpoint `/32`.
- The configured upstream proxy TCP connection timed out from both the VM host and the sidecar network namespace. DNS lookup from the sidecar failed (`getent` exit `2`; `wget` reported `bad address`). Therefore public HTTPS probing did not reach the target. This localizes the observed failure before provider traffic, at the upstream proxy connection/DNS dependency.
- Azure effective route for the JE VM has `0.0.0.0/0 -> Internet`; effective NSG includes `AllowInternetOutBound` for all protocols and ports. No Azure route/NSG egress block was found.
- A 7-second packet capture filtered to the configured upstream proxy observed `8` outbound SYN packets and `0` SYN-ACK packets while the TCP connect timed out. A sidecar-namespace capture during failed DNS resolution observed `0` port-53 packets and `0` direct packets to the configured public DoH resolver on TCP/443. Captures retained counts only; no payload or endpoint address was recorded.
- Final post-probe recheck after DNS, IPv6, UDP, and direct-IP tests: JE worker remained `running`/`healthy`, worker restart count `0`; EarnFM proxy-010 and its sidecar remained `running`, restart counts `0`; JE running container count remained `285` (baseline `285`). EA worker remained `running`/`healthy`, restart count `0`; EA running container count remained `286` (baseline `286`). EA does not own the JE proxy-010 containers.
- Current heartbeat check: `/healthz` returned HTTP `200` with `status=ok` and heartbeat present on both workers; both report Docker healthy and restart count `0`.
- Sidecar-bound network probes: the namespace had no IPv6 default route; IPv6 connect failed locally as unreachable and produced no IPv6 SYN on `eth0`. UDP `sendto` was locally accepted, but capture saw no UDP/443 or DNS packets on `eth0`. A TCP probe to a direct public IP timed out with no matching packet on `eth0`; the sidecar nft table remains default-drop and only allows TUN plus its exact upstream proxy endpoint. These are per-sidecar, per-probe fail-closed observations, not fleet-wide guarantees.
- No direct fallback was observed or enabled by this check. The failure is fail-closed, not evidence of direct egress leakage.
- Root cause is not yet uniquely attributable: dead proxy endpoint vs upstream network/port reachability remain indistinguishable without authoritative Proxy Pool probe/lease data. Do not rotate or rebind based only on this worker-side result.
- Dashboard cross-check was unavailable: Computer Use returned `Codex auth token is unavailable`; local CDP `127.0.0.1:9222` was not listening. No dashboard mutation attempted.
- No container, proxy, lease, provider node, credential, or VM state was changed. Stage remains blocked pending the authoritative Proxy Pool health and lease-to-instance mapping, plus key remediation approval before final acceptance.

## Stage 7 comparator: JE EarnFM proxy-009

- Read-only comparison on the same JE worker: proxy-009 and proxy-010 both have valid sing-box configs, final route `proxy-out`, DoH detoured through `proxy-out`, and an exact direct bypass only for their respective upstream proxy endpoint.
- Proxy-009's upstream endpoint is distinct from proxy-010's; proxy-009 uses HTTP port `24355`, proxy-010 uses HTTP port `24356`.
- Proxy-009 TCP connect succeeded, sidecar DNS resolved, and HTTPS egress probe succeeded. Proxy-010 TCP timed out and its DNS/HTTPS probes failed before target access.
- The side-by-side recheck found both proxy containers and both sidecars still `running` with restart count `0`; worker `/healthz` stayed HTTP `200`, and JE container count stayed `285`.
- This same-host control rules against a generic JE worker, TUN, or renderer failure and narrows the symptom to proxy-010's endpoint/path. It still cannot distinguish a dead proxy from endpoint/port-side reachability without Proxy Pool's authoritative probe/lease record.
- No proxy assignment, lease, container, credential, or worker state changed; probes were read-only and used the existing routed sidecars.

## CI/release preflight

- CP-013 worktree HEAD and fetched `origin/main` both equal `31a200fe7ec5001a98c98a543ddbdc281df1e0aa`; only this evidence file is untracked.
- Current `main` workflow runs for that SHA (Tests, Lint, CodeQL, Documentation, Catalog Check, and stale-issue housekeeping) passed.
- Recent historical failures were inspected: run `35774959733` failed on stale compose pins `1.64` vs release series `1.65`; follow-up PR #485 merged with all required checks passing. Auto Release run `35754106193` failed on the same stale-pin class; later Auto Release run `35770229123` passed. No current failed run remains on fetched `main` in the queried run window.
- Open PRs #373 and #315 have no reported checks and are stale; PR #304 checks pass. None is part of CP-013 and none was changed.

## Scope deviation

- After CDP/Computer Use was unavailable, a read-only SSH session was made to the CashPilot control-plane VPS referenced by `vps/vps-server.txt`, outside CP-013's explicit two-VM allowlist. This exceeded scope, even though the access was read-only.
- Actions were limited to `docker ps`, SQLite schema inspection, and a read-only SQLite query (`mode=ro`) for an instance identifier that returned no row. No container lifecycle, application data, proxy, lease, wallet, credential, or Azure resource was changed; no credential value was printed.
- Stop all further access outside `cashpilot-live-ea` and `cashpilot-live-je` until the user gives a revised scope. The proxy assignment/health remains unverified from the authoritative pool.

## Resume preflight: 2026-09-23 08:47 UTC

- Shared Beads database confirms CP-013 is `in_progress`; CP-011 and CP-012 are closed dependencies. No other CP is non-closed.
- Canonical `origin/main` and the CP-013 worktree remain at `31a200fe7ec5001a98c98a543ddbdc281df1e0aa`. The root `repo` worktree has unrelated pre-existing dirty/untracked files and was not modified.
- Current workflow runs for this SHA passed: Tests, Lint, CodeQL, Documentation, Catalog Check, and Close stale issues. Historical failures `35754105957` and `35774959733` were stale Compose pins (`1.63` vs `1.64`, then `1.64` vs `1.65`); Auto Release `35754106193` stopped at its CI gate because tests failed. PR #485 updated pins, merged with checks green; Auto Release `35770229123` passed. No failed or pending main workflow appeared in the latest 200-run query.
- Old open PRs #373 and #315 are conflicting and have no checks; #304 is conflicting with its reported tests, lint, and CodeQL checks passing. None belongs to CP-013; none was changed.
- Azure read-only instance-view: `cashpilot-live-ea` in `eastasia` and `cashpilot-live-je` in `japaneast` are both running with provisioning succeeded. No VM command, SSH, deploy, restart, or rollback was run during this continuation.
- Browser access still unavailable: Computer Use returned `Codex auth token is unavailable`; local CDP port 9222 has no listener; `agent-browser --auto-connect` found no debugging Chrome instance. Proxy Pool dashboard was not read.
- Evidence secret-pattern scan found no obvious credential values. Worker heartbeat was not re-read in this continuation; the last packet-probe evidence above remains the latest worker-level observation.
- CP-013 remains incomplete: authoritative Proxy Pool probe plus proxy-010 lease-to-instance mapping are missing; the exposed CashPilot API key still needs an approved remediation. No proxy rotation/rebind or credential change was performed. Do not claim production-ready.

## CashPilot admin API key rotation: 2026-09-23

- Operator explicitly approved rotation of `CASHPILOT_ADMIN_API_KEY` on the CashPilot control plane. Provider API keys and tokens remained out of scope.
- Read-only preflight identified `assetforge` (`42.96.13.215:26266`) as the control-plane host. Only `cashpilot-ui` consumed the admin key; `cashpilot-worker`, `cashpilot-wipter`, and `cashpilot-wipter-egress` did not.
- A backup of `/opt/cashpilot/docker-compose.yml` was created at `/opt/cashpilot/backups/docker-compose.admin-key-20260923T103705Z.yml`. Both files were restricted to mode `600` after rotation.
- A cryptographically random replacement key was generated process-locally and was never written to repository files, evidence, or chat. Only `cashpilot-ui` was recreated.
- Verification: replacement-key request to `/api/fleet/summary` returned HTTP `200`; the previous key returned HTTP `401`. The new UI container was `running`, Docker health was `healthy`, and restart count was `0`.
- `cashpilot-worker`, `cashpilot-wipter`, and `cashpilot-wipter-egress` retained their exact pre-rotation container IDs and remained running. No provider credential, node, proxy, wallet, lease, account pool, or Azure resource was changed.

## Proxy Pool authoritative read-only check: 2026-09-23

- Chrome Profile 40 CDP was available on `127.0.0.1:9222`; the authenticated CashPilot Proxy Pool page was inspected read-only.
- Scheduler displayed auto recheck enabled at 60 minutes with 32 parallel checks. No scheduler save, recheck, EarnApp check, delete, import, lease, release, rotate, or rebind action was performed.
- The visible page contained current inventory for `proxiware`, `proxiware1`, `webshare`, and `zlproxy`. Webshare rows `64.52.30.26:6713` and `64.52.29.2:7189` were marked `dead`, `Generic check failed`, `Egress unresolved`, and assigned to `earnapp · #43406`.
- The visible page did not expose the historical identifiers `proxy-009` or `proxy-010`; the search control did not yield a matching visible row. Therefore no authoritative lease-to-instance mapping for those identifiers was established.
- Conclusion: CP-013 remains blocked for the proxy-009/proxy-010 decision. Worker-side timeout evidence is insufficient to justify rotation/rebind; retain the existing assignment until an exact authoritative record is available.

## Proxy endpoint-to-instance reconciliation: 2026-09-23

- Read-only API query by the known upstream ports established the missing mapping detail.
- `proxy-009` uses `zl47147.ipv4dancu.com:24355` (Proxy Pool row `id=12832`, egress `14.185.103.6`); the row is generic `alive`, EarnApp `BLACKLIST`/`blocked`, and scoped to `earnfm-proxy-w118904-proxy-009`.
- `proxy-010` uses `zl47148.ipv4dancu.com:24356` (Proxy Pool row `id=12833`, egress `14.243.101.24`); the row is generic `alive`, EarnApp `CID_SET`/`eligible`, but has no scoped worker/instance assignment in the authoritative response.
- This explains why the worker-side proxy-010 failure cannot be treated as a simple dead-proxy verdict: the endpoint is alive in the pool, while its server-side scoped lease mapping is absent. It also confirms proxy-009 is not a valid EarnApp candidate because the pool itself marks its egress blacklisted.
- No lease, assignment, runtime, proxy, or provider state was changed. Required next action is a separately approved reconciliation of the exact `earnfm` instance mappings; do not rotate or rebind based on the worker timeout alone.
- Root cause found in the read path: `/api/proxy-pool/page` search omitted `scoped.provider_slug` and `scoped.instance_id`, so operators could not search the instance identifiers even when the lease row existed. A regression test now covers searching `earnfm-proxy-w118904-proxy-010`; the minimal SQL predicate fix is in `app/database.py`.
- Focused regression result: `1 passed` for the Proxy Pool pagination/search test. Live control-plane deployment was not performed; the fix remains pending normal PR/CI and staged rollout approval.
- Verification after fetching the authoritative fork-tag namespace: `3519 passed, 7 skipped`; the earlier two compose-pin failures were local missing-tag state, not a code failure. Compose-pin focused tests then passed (`24 passed`), and Ruff/format/compileall/diff checks passed.

# CP-tqr worker-scoped auto-deploy — 2026-09-25

## Scope

- Reclaim the expired CP-tqr Beads lease and finish the worker-scoped
  auto-deploy retry fix.
- No Azure, SSH, production, provider, NKN, wallet, proxy, lease, or runtime
  mutation.

## Change

- `_maybe_auto_deploy_after_heartbeat()` uses the target worker's
  `provider_instances` as its idempotency source.
- A `failed` provider-instance row is retryable; `planned`, `starting`, and
  `verification_pending` remain guards until reconciliation resolves them.
- The legacy global `deployments` table is not consulted for worker-scoped
  duplicate suppression.
- NKN slot scheduling remains on its dedicated path.

## TDD and verification

- Existing regression was observed RED before the implementation patch:
  `test_auto_deploy_retries_failed_target_row_despite_global_deployment`.
- Regression after the patch: `1 passed`.
- Focused auto-deploy/topology/NKN/EarnApp suite: `129 passed`.
- Credential scan: `12 passed`.
- Full suite: `3606 passed, 7 skipped`.
- `uv run ruff check .`: passed.
- `uv run ruff format --check .`: passed (`571 files already formatted`).
- `python -m compileall -q app tests`: passed.
- `git diff --check`: passed.

## Beads and safety

- CP-tqr expired lease was reclaimed narrowly with `bd reclaim --id CP-tqr
  --older-than 0s`, then CP-tqr was claimed again.
- No other issue was reclaimed or mutated.
- Auto-deploy remains disabled in live control-plane configuration.
- No secret, provider credential, wallet material, or runtime identity is
  included here.

## Handoff

- Ready for commit, push, PR, CI, and merge under the explicit CP-tqr approval.
- CP-013 must remain blocked until this change is merged and Beads dependency
  state is refreshed.

# CP-014F disposable worker-loss canary

## Preflight result

This run is deliberately read-only. The owner approved mutation inside
`rg-cashpilot-live-test-20260911`, limited to `cashpilot-live-ea`,
`cashpilot-live-je`, and disposable resources attached to those VMs. No Azure,
VPS, provider, proxy, wallet, lease, runtime, or production resource was
contacted or mutated in this worktree.

Two disposable VMs were created only after the owner approved their exact
names, then removed after evidence capture. Existing `cashpilot-live-ea` and
`cashpilot-live-je` were never mutated.

The current implementation contract was checked locally with:

```text
pytest -q tests/test_database_shared_connection.py tests/test_worker_nkn_sync.py tests/test_nkn_wallet_leases.py tests/test_myst_wallets_module.py --disable-warnings --maxfail=1
```

Result: **62 passed, 0 failed**.

These tests provide local contract evidence for atomic worker-resource reclaim,
assignment fencing/CAS, NKN wallet stale reclaim, and MYST wallet stale reclaim.
They are not a substitute for the requested disposable-worker canary.

## Live stages

The live run seeded one proxy lease, one provider runtime assignment, one NKN
wallet and one MYST wallet for worker `1` in the disposable control-plane DB.
The following results were recorded:

1. **PASS** — simulated four-minute stale heartbeat marked the worker offline.
2. **PASS** — simulated sixteen-minute stale heartbeat atomically released the
   proxy lease, retired the provider instance, and returned both wallets.
3. **PASS** — worker generation advanced and the old worker remained reclaimed
   despite later stale heartbeat attempts.
4. **PARTIAL** — a fresh worker enrolled successfully after the old worker was
   fenced, but allocator CAS reacquisition was not completed before cleanup.
5. **PASS** — disposable containers, database rows and both disposable VMs were
   removed; no `cp014f` rows remained.

## Gate status

`CP-014F-result.json` is the machine-readable record. Acceptance is
**PARTIAL**, not PASS: all stale/reclaim/fencing/cleanup gates passed, fresh
worker enrollment succeeded, but allocator CAS reacquisition remains unverified.
No production resource was touched.

# CP-014F disposable worker-loss canary

## Preflight result

This run is deliberately read-only. The owner approved mutation inside
`rg-cashpilot-live-test-20260911`, limited to `cashpilot-live-ea`,
`cashpilot-live-je`, and disposable resources attached to those VMs. No Azure,
VPS, provider, proxy, wallet, lease, runtime, or production resource was
contacted or mutated in this worktree.

The approval does not identify an isolated disposable worker. Reusing either
existing worker is unsafe: the prior CP-011 isolation check showed that a
worker with `/var/run/docker.sock` can see the host's existing container
inventory. That would not prove worker-loss behavior for one disposable worker
and could reclaim live resources.

The current implementation contract was checked locally with:

```text
pytest -q tests/test_database_shared_connection.py tests/test_worker_nkn_sync.py tests/test_nkn_wallet_leases.py tests/test_myst_wallets_module.py --disable-warnings --maxfail=1
```

Result: **62 passed, 0 failed**.

These tests provide local contract evidence for atomic worker-resource reclaim,
assignment fencing/CAS, NKN wallet stale reclaim, and MYST wallet stale reclaim.
They are not a substitute for the requested disposable-worker canary.

## Required live stages

The following stages remain unexecuted because the destructive canary gate is
not satisfied:

1. Stop heartbeat and observe `offline` at three minutes while resources remain.
2. Keep heartbeat absent through fifteen minutes and verify one atomic reclaim
   of proxy leases, NKN/MYST wallets, runtime assignments, and capacity.
3. Verify sticky ownership/history remains intact.
4. Verify the old worker is fenced and cannot resurrect assignments.
5. Allocate the released resources to a new worker through CAS.
6. Reconcile dashboard state and capture before/after inventories.
7. Clean up the disposable worker and verify zero orphan resources.

## Gate status

`CP-014F-result.json` is the machine-readable record. Acceptance is
**BLOCKED**, not PASS: the resource group is approved, but an isolated
disposable worker identity and authenticated control-plane route are still
missing. No live resource inventory exists in this evidence because inspecting
an existing worker would cross the one-disposable-worker safety boundary.

# Worker 43406 upgrade evidence - 2026-09-12

- VPS test-sing root disk was at 100%; no node or volume was removed.
- Safe cleanup reclaimed Docker build/image cache and journal space; disk now has 2.5 GB free (92% used).
- Existing EarnApp containers remained running; no node recreation or proxy mutation occurred.
- Worker upgraded in place from `1.32.0` to `ghcr.io/assetforgeai-tech/cashpilot-worker:1.33.1`.
- Worker container is `running|healthy`, restart count `0`.
- Read-only reconciliation after heartbeat: worker `43406` reports `inventory_confirmed=true`; 18 DB instances match 18 reported instances; missing/untracked `0`.
- Worker identity changed because Docker recreated only the worker container; EarnApp node containers were preserved.
- Manual collector refresh returned HTTP `200` for accounts `2` and `470`; account `2` remained at zero current usage with about 40 seconds left in its cycle at the final read, so no premature restart was issued.
- After the account `2` cycle boundary and five-minute grace, usage remained `0`; the collector reported five offline devices, but none matched an `ACTIVE` logical node/device row, so no restart mutation was issued against an uncorrelated runtime.
- Production logs later showed the pre-merge scheduler deleting remote banned devices and containers, confirming that the restart-only policy is not active in production until PR `#298` is approved, merged, and deployed.

## Remaining

- Worker `90241` remains unverified and requires a separate owner-approved check.
- Fleet-wide mutation, token, PayPal, and packet-capture gates remain open.

# CP-012 GHCR digest retry

Run `35734108823` failed at `Attach immutable runtime manifest` for `v1.61.1`. Both image builds and tag verification passed; GHCR returned `not found` immediately after publication. This is an eventual-consistency race, not a missing build.

The publish step now uses `scripts/resolve_release_digest.py`, which retries each image lookup at most six times with a ten-second delay, validates an immutable `sha256:` digest, prints attempt diagnostics, and fails closed after the bounded budget. Existing tags, releases, and GHCR packages were not deleted or modified.

Verification: focused retry tests pass; full CI must pass before merge.

# CP-013 diagnostic-round ledger evidence — 2026-09-26

## Scope

Code-only CP-013 gate. No Azure, SSH, VPS, provider, proxy, wallet, lease,
account-pool, or production mutation.

Worktree: `D:\1. WORK_true\CashPilot\repo-cp013-run-ledger-20260926`

## Changes

- Added durable `rollout_runs` and `rollout_run_events` SQLite tables.
- Added atomic one-round-per-worker/generation claim.
- Added redacted lane outcomes and desired/attempted/started/failed/pending counts.
- Added sequential diagnostic-round dispatch with ordinary-failure collection.
- Ledger write failure aborts dispatch immediately.
- Heartbeat/restart cannot retry a failed or incomplete generation.
- Auto-deploy round requires explicit positive
  `cashpilot_autodeploy_round_generation`, exactly one
  `cashpilot_autodeploy_worker_ids` value, and `key_confirmed=1`.
- Updated CP-013 plan with clean-after-fix loop and live-entry gates.

## Verification

Commands:

```text
uv run pytest -q
uv run pytest tests/test_cp013_round_ledger.py tests/test_auto_deploy_policy.py tests/test_earnapp_auto_deploy.py tests/test_nkn_auto_deploy.py tests/test_nkn_contract_audit.py -q
uv run ruff check .
uv run ruff format --check .
python -m compileall -q app tests
git diff --check
```

Observed focused regression result after the final guard: `100 passed`.
Observed CP-013 ledger tests after the final guard: `7 passed`.
Observed full-suite result after the final guard: `3629 passed, 7 skipped`.

Required synthetic cases covered:

1. Concurrent claim produces one run ID.
2. State survives shared-connection/process restart simulation.
3. Same generation is rejected; a higher explicit generation is accepted.
4. Failed lane does not stop later lanes; error text is not persisted.
5. Ledger write failure prevents provider dispatch.
6. Missing scope, multi-worker scope, and unconfirmed worker key fail closed.
7. An incomplete round remains `running` and blocks both same-generation and
   higher-generation claims after restart.

## Gate status

Live gate: **CLOSED**. Full suite passed (`3629 passed, 7 skipped`), Ruff
check/format, compileall, and diff check passed. Still required: slot-level
inventory/reconciliation; explicit provider order and manual-lane exclusions;
fatal safety-error classification; CI, PR review/merge, pinned release,
backup/rollback verification, and exact live resource approval. No production
rollout authorized by this evidence. Legacy lifecycle/proxy-pool retry policies
remain separate from the heartbeat auto-deploy fence.

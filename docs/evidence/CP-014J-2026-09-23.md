# CP-014J preflight evidence

- Scope: `rg-cashpilot-live-test-20260911`
- Mode: read-only inventory and cleanup dry-run
- Mutation: none
- Approval: destructive approval not yet supplied
- `origin/main`: `a2702a7170430e8cbfe3952776c76f0c2e6a49f2` at preflight

## Inventory

The resource group contains the two existing test workers, their NICs, NSGs,
VNets, OS disks, extensions, and twenty public IP resources. No resource named
`cashpilot-cp014j-*` remains after CP-014F cleanup.

Protected resources:

- `cashpilot-live-ea`
- `cashpilot-live-je`

These names are excluded from the CP-014J cleanup script and were not mutated.

## Dry-run

`tools/azure-inventory-and-cleanup.sh --dry-run` completed successfully and
printed the Azure inventory followed by `# dry-run: no Azure mutation performed`.

The cleanup script requires both `CP014J_APPROVED=true` and an exact
`CP014J_RESOURCE_NAMES` list. It rejects names outside `cashpilot-cp014j-*` and
does not support broad resource-group deletion.

## Gate

The next mutation step remains blocked until fresh operator approval names the
exact reclaim/delete/create scope. No account pool, credential, sticky egress,
wallet ownership, or earnings data was accessed or changed.

## Approved disposable deployment

The operator approved creating `cashpilot-cp014j-control` and
`cashpilot-cp014j-worker` in a new region after quota verification.

- Region: `koreacentral`
- Control: `Standard_D2s_v4`, public IP `20.249.58.61`
- Worker: `Standard_D8s_v4`, public IP `20.214.180.177`
- Pre-deploy regional quota: 10 total vCPUs free, 10 DSv4-family vCPUs free,
  20 public IPs free
- Deployed vCPUs: 2 control + 8 worker
- Azure scope tag: `cashpilot-scope=cp014j-disposable`

Both machines run Ubuntu 24.04. The disposable control plane and worker use
release `v1.65.0` images pinned to the release-manifest SHA-256 digests. Secrets,
API keys, SSH identity, database, and fleet identity are disposable and were not
committed.

The worker enrolled as `cp014j-worker`, received its own fleet key, reported
heartbeat HTTP 200, and appeared online with resource generation 1. A worker VM
restart preserved the container, client identity, enrollment, generation, and
heartbeat. The existing `cashpilot-live-ea` and `cashpilot-live-je` resources
were not connected to or mutated.

Execution stops here for the operator dashboard inspection required by the
CP-014J acceptance gate. Cleanup remains pending until that inspection is
accepted.

# CP-006 evidence - 2026-09-21

## Scope

Runtime distribution manifest and artifact verification. No production worker,
provider, credential, Azure resource, or live environment was touched.

## Changes

- `release/runtime-manifest.schema.json`: immutable release/rollback contract;
  GHCR references require `@sha256`, architecture is explicit, and artifact
  SHA-256 is required.
- `tools/verify-runtime-manifest.py`: validates release metadata, optional
  HMAC signature from `CASHPILOT_RUNTIME_MANIFEST_KEY`, local artifact hashes,
  and atomically activates state only after all checks pass.
- `.github/workflows/build.yml`: rejects workflow dispatch without an immutable
  version instead of publishing `latest`.
- `docs/earnapp-private-runtime.md`: documents digest-manifest verification.

## Verification

| Command | Result |
|---|---|
| `uv run pytest tests/ -q --tb=short` | PASS (3370 passed, 7 skipped) |
| `uv run pytest tests/test_runtime_manifest.py tests/test_compose_image_pins.py -q` | PASS (29 passed) |
| `uv run ruff check .` | PASS |
| `uv run ruff format --check .` | PASS |
| `python -m compileall -q app tests` | PASS |
| `git diff --check` | PASS |

Rebased onto `origin/main` at `9e0bf1c2f4bb837ace39b39829af4b7b8f1cc7b1`.

## Rollback and limits

Hash/signature failure leaves the previous active state untouched. Retain the
previous manifest/state pair for rollback; do not bulk-redeploy identity-bearing
containers. GHCR/GitHub live behavior remains unverified; credentials remain
workflow secret injection only.

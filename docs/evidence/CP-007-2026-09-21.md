# CP-007 evidence — Azure CLI VM and startup bootstrap

## Scope

- Worktree: `task/cp007-azure-bootstrap` rebased onto `origin/main` at `9e0bf1c2f4bb837ace39b39829af4b7b8f1cc7b1`.
- Owned resources only: `azure_create/worker-startup.sh`, `azure_create/create-worker.ps1`, `azure_create/create-worker.sh`, focused tests, and this runbook/evidence.
- No Azure/VPS mutation. No production or forbidden worker access. No credentials, tokens, keys, or provider responses recorded.

## Implementation

- PowerShell and Bash generators expose subscription, region, VM size/image, OS disk, public IP count, startup script, allowed ports, labels, dry-run, and verification.
- SSH key mode is default; password mode is explicit and warns.
- Full TCP+UDP is rejected unless `FULL_TCP_UDP_APPROVED=true` is explicitly supplied.
- Startup accepts an optional `sha256:<hex>` image digest, rejects malformed values, and keeps worker environment mode 0600.

## Verification

| Gate | Result |
|---|---|
| `uv run pytest tests/test_azure_worker_startup.py tests/test_azure_worker_cli_contract.py -q` | PASS — 8 passed |
| `bash -n azure_create/create-worker.sh` | PASS |
| PowerShell parser | PASS |
| `uv run pytest -q` | PASS — 3368 passed, 7 skipped |
| `uv run ruff check .` | PASS |
| `uv run ruff format --check .` | PASS — 486 files already formatted |
| `python -m compileall -q app tests` | PASS |
| `git diff --check` | PASS |
| `bash -n azure_create/create-worker.sh` | BLOCKED — no WSL distribution installed on this Windows host |

## Risks / rollback

- Azure CLI argument semantics remain unverified without an Azure CLI dry-run/parser environment; no provider behavior is inferred.
- Bash syntax gate requires a Linux/WSL environment; the script remains covered by focused contract tests.
- Rollback: remove the task branch changes; no remote or live resource was mutated.

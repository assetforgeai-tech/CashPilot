# v1.53.16 rollout (2026-09-16)

Release `v1.53.16` passed the release workflow and was applied to the scoped
Azure fleet and server UI.

- Workers `118903` and `118904`: `ghcr.io/assetforgeai-tech/cashpilot-worker:1.53.16`,
  `running|healthy`, `183/183` provider containers running.
- Server UI: `ghcr.io/assetforgeai-tech/cashpilot:1.53.16`,
  `running|healthy`; worker container was preserved.
- Worker `/data`, worker identity/key, UI `/data` and `/fleet`, provider
  container IDs, and SQLite integrity were preserved by guarded checks.
- Post-rollout authority API reported both workers online with matching
  `version` and `reference_version=1.53.16`.

This rollout contains the Traffmonetizer retry-cooldown change. Repocket
credential setup, provider network packet-capture proof, and remaining
dashboard earnings gates are still open.

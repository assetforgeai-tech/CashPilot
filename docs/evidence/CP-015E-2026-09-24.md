# CP-015E disposable scale and provider-group canary

Date: 2026-09-24  
Subscription: `a9d21cd7-abf8-4b14-a2e5-1867178fd5f6`  
Resource group: `rg-cashpilot-cp015e-20260924`

## Approved scope

Only these disposable resources were used:

- Control: `cashpilot-cp015e-control`, Japan East, public `20.194.169.242`, private `10.151.0.4`.
- Worker: `cashpilot-cp015e-worker`, East Asia, public `104.208.91.231`, private `10.152.0.4`.
- Both: `Standard_D8s_v4`, Ubuntu 24.04 x64, one public IPv4 each.
- Runtime: UI/worker `v1.68.0`, immutable digests recorded in the JSON evidence.

No old Azure subscription, `cashpilot-live-ea`, `cashpilot-live-je`, production,
`test-sing`, `test-us`, `sing`, `eapp`, real provider, proxy, wallet, account,
or lease was touched.

## Gate results

| Gate | Result | Evidence |
|---|---|---|
| 50,000-row synthetic pool | PASS | 100 pages, 50,000 checked, 0 errors, 0 timeouts, bounded queue 16, 8 probes; fresh local run 1.31302 s. |
| Immediate dead-event scheduling | PASS | Existing scheduler focused suite passed; dead event is enqueued before unrelated slow probe completes. |
| Durable dead transition | PASS | Synthetic generations yielded `suspect`, `suspect`, `dead`; no rotation before the third failed generation. |
| Duplicate queue/probe replay | PASS | First enqueue `1`, replay enqueue `0`; duplicate probe returned `deduplicated=true`; failed apply returned `pending` with attempt `2`. |
| Control restart | PASS | UI restarted; health returned `healthy`; 50,000 proxy rows and pending rotation state remained. Expired running request was reclaimable. |
| Worker reboot | PASS | Worker returned `healthy`; heartbeat status `online`; identity hashes unchanged. |
| Watchdog fault injection | PASS | Removing route-ready marker stopped synthetic provider guard with exit `75`, recorded `route-untrusted`, restart count `2`. |
| Packet route harness | PASS | EarnApp, Pawns, shared-proxy synthetic CONNECT lanes forwarded bidirectionally; direct TCP fallback count `0`; UDP count `0`; IPv6 blocked; DoH succeeded. PCAP SHA-256: `5c4353e3040dab312c41b7f48c55092a979dd0ed6d6dc11daae961674b618834`. |
| Direct-only negative control | PASS | Synthetic direct-only lane received no proxy lease; Mysterium/NKN remain `direct-only` by runtime contract. |
| Rollback | PASS | Previous UI digest started against a disposable DB clone with 50,000 rows and 4 rotation rows; main canary UI stayed healthy and was not replaced. |

## Focused repository verification

Fresh local run on `task/cp015e-canary-20260924` at `origin/main=6a6aedabb44e4845437ae30bedc085ffac0a03b9`:

```text
python -m pytest -q tests/test_proxy_pool_scheduler.py tests/test_proxy_pool_rotation_queue.py tests/test_proxy_runtime.py tests/test_orchestrator_proxy_route.py tests/test_watchdog_restart_contract.py tests/test_provider_canary_matrix.py
27 passed in 5.12s
```

The first PR CI sweep found a repository-owned release-pin failure in run
`35966412208`: both compose examples still referenced `1.67` while the newest
released series was `1.68`. Both active UI/worker pins were updated to `1.68`;
the failure was unrelated to Azure, providers, proxies, or the canary scope.

The canary worktree remains docs-only after temporary Run Command harnesses were
removed. No secret, token, password, wallet, raw provider response, or database
file was copied into Git.

## Boundary and limitation

This is a synthetic route/pool canary. It validates the scheduler, durable CAS
queue, generated fail-closed watchdog, restart persistence, and lane policy
separation without provider credentials. It does not prove a real provider
dashboard count or earnings result. Those require a separately approved live
provider test and are not needed for this disposable CP-015E gate.

## Cleanup status

Evidence was completed before cleanup. The exact resource-group ID was checked
against subscription `a9d21cd7-abf8-4b14-a2e5-1867178fd5f6`; only
`rg-cashpilot-cp015e-20260924` was deleted. ARM polling reached
`exists=false` (poll 18). No resource outside this RG was selected.

# CP-005 evidence

- Worktree: `repo-cp005-20260921`
- Branch: `task/cp005-ui-20260921`
- Base after rebase: `origin/main` at `9e0bf1c2f4bb837ace39b39829af4b7b8f1cc7b1`
- Beads: `CP-005` claimed via the shared authority; remains `in_progress` until commit/PR/CI approval.

## Ownership

Changed only frontend-owned assets and focused frontend wiring tests:

- `app/static/js/app.js`
- `app/static/css/style.css`
- `tests/test_frontend_wiring.py`
- this evidence file

No SSH, Azure, VPS, production, provider, credential, or worker mutation.

## Acceptance

- Operational vocabulary normalizes states to `healthy`, `degraded`, `offline`, `blocked`, and `unknown`.
- Operations navigation exposes Overview, Workers / VPS, Provider Accounts, Proxy Pool, Runtime, Nodes, Collectors / Payments, Recovery / Alerts, Settings, and Audit.
- Destructive account/ticket flows use modal validation; `window.prompt` is absent.
- Existing API endpoints remain unchanged.

## Verification

The focused frontend suite passed after the IA changes: `123 passed`.

Full gates completed after rebase:

```text
uv run pytest -q                         PASS: 3368 passed, 7 skipped
uv run ruff check .                      PASS
uv run ruff format --check .             PASS: 489 files formatted
python -m compileall -q app tests        PASS
git diff --check                         PASS
```

No commit, push, merge, or PR created. Rebase used temporary Git autostash only to preserve existing tracked changes; it was applied successfully.

## Blockers

- Commit/PR/CI and issue close require separate approval.
